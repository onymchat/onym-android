<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <!--
        Required by `GitHubReleasesKnownRelayersFetcher` (pulls
        `relayers.json` from the latest release of `onymchat/onym-relayer`)
        and by `GitHubReleasesContractsManifestFetcher` (same pattern for
        the contracts manifest). Without it, OkHttp throws SecurityException
        on every call and the relayer picker shows "could not reach
        published list" forever — this was the visible symptom on cold
        install before this fix.
    -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
        CAMERA: the invite-QR scanner on the Create Group → "Invite by
        inbox key" screen (`QrScannerScreen`) reads a contact's
        `https://onym.app/i?k=…` identity-invite QR via CameraX.
        Requested at runtime (API 23+) the first time the user taps
        "Scan QR"; denial degrades to the existing paste-the-key path.

        `uses-feature ... required="false"`: a camera is not essential
        to the app — devices without one (or where the user declines)
        still use manual key entry — so we don't want Play Store to
        filter the listing on camera hardware.
    -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="false" />

    <!--
        RECORD_AUDIO: recording voice messages in the chat composer
        (`ChatVoiceRecorder`). Requested at runtime the first time the user
        holds the mic button; audio is AES-GCM-encrypted on-device before
        upload. Microphone hardware isn't essential (text + media messaging
        works without it), so the feature is declared not-required.
    -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="false" />

    <!--
        POST_NOTIFICATIONS (API 33+): the local, content-free "New
        message" notification `PushMessagingService` renders on an FCM
        wake. Requested at runtime by the Settings → Notifications
        toggle; the push seat is opt-in and OFF by default, and a
        denial simply leaves it off. Below API 33 the permission is
        granted by install.
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".OnymApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@android:style/Theme.Material.Light.NoActionBar"
        android:supportsRtl="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:allowBackup="false"
        tools:targetApi="34">

        <!--
            allowBackup="false": the EncryptedSharedPreferences blob holds
            the user's nostr secp256k1 secret + BLS12-381 secret. Even
            though they're encrypted at rest with a Keystore-bound master
            key, Google Drive backups would migrate the encrypted blob
            without the matching key — best to opt out entirely so users
            don't end up with a "restored" device that has unrecoverable
            ciphertext.
        -->

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
                Universal "App Link" entry point for Level-2 invite
                deeplinks (PR-6/7 in the deeplink-invite stack).

                `autoVerify="true"`: Android fetches
                https://onym.app/.well-known/assetlinks.json on
                install; if the SHA-256 cert fingerprint matches our
                signing cert (40:AD:4E:78:…:CAD), the system opens
                the link directly in onym-android without the
                disambiguator chooser. assetlinks.json is already
                served (verified live) — no server change needed.

                Custom-scheme `onym://join` filter is the fallback for
                pre-verification states (e.g., install-via-sideload
                before assetlinks fetch completes) and for direct
                test invocations from adb / shell.
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https"
                      android:host="onym.app"
                      android:pathPrefix="/join" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="onym" android:host="join" />
            </intent-filter>
        </activity>

        <!--
            The receiving end of the content-free push wake. FCM
            requires the MESSAGING_EVENT filter; not exported — only
            Google Play services delivers to it. The service is
            deliberately graph-free (see its KDoc): it renders a local
            notification from string resources and relays token
            rotations through PushTokenRelay, nothing else. Harmless
            when the push seat is dark or google-services.json is
            absent — no FCM delivery ever occurs then.
        -->
        <service
            android:name=".PushMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Grants a share target read access to one exported proof,
             and nothing else: the authority is scoped to the
             `rules-proof/` cache subdirectory. `grantUriPermissions`
             plus the per-Intent flag means the grant dies with the
             receiving activity rather than standing open. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.rulesproof"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/rules_proof_paths" />
        </provider>
    </application>
</manifest>
